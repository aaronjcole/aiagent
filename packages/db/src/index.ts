/**
 * @app/db — Prisma client singleton + re-exported generated types/enums.
 * Import enums (ProspectStatus, DraftStatus, ...) and model types from here.
 */
export { prisma } from './client.js';
export * from '@prisma/client';
